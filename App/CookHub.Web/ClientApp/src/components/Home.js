import React from 'react';

const Home = props => (
    <div>
        <h1>Hello, world!</h1>
        <p>Welcome to your new single-page application, built with</p>
    </div>
);

export default (Home);
